@page "/logout"
@inject CustomAuthStateProvider AuthStateProvider
@inject NavigationManager Navigation

@code {
    protected override async Task OnInitializedAsync()
    {
        AuthStateProvider.SetUnauthenticatedUser();
        Navigation.NavigateTo("/");
    }
}